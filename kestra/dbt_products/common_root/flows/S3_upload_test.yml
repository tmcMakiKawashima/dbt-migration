id: S3_upload_test
namespace: kestra_dbt_project
tasks:
  - id: S3_upload
    type: io.kestra.plugin.aws.s3.Upload
    from: "{{ outputs.dbt_docs_generate['outputFiles']['enterprise_data_products/target/static_index.html'] }}"
    bucket: "{{ namespace.DBT_DOCS.S3_BUCKET }}"
    key: "{{ namespace.DBT_DOCS.BASE_PATH }}{{ inputs.branch }}/static_index.html"
    contentType: text/html
    # accessKeyId: "{{ secret('AWS_ACCESS_KEY_ID') }}"
    region: ap-northeast-1
    # secretKeyId: "{{ secret('AWS_SECRET_ACCESS_KEY') }}"
    # stsRoleArn: arn:aws:iam::248810166614:role/da-dxpf-edp-role-task-kestra01
    # # SessionNameに関しては現状何でも良い認識です
    # stsRoleSessionName: kestra-s3-session 
    # stsRoleExternalId: kestra-s3
    # stsRoleSessionDuration: "900"