version: 2

models:
# Y品番マスタ
  - name: stg_yhinban_master_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: y_item_no_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# Y品番-仕入先マスタ
  - name: stg_yhinban_shiiresaki_master_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: y_item_no_supplier_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# ガスボンベ保管場所履歴
  - name: stg_gashokanbasyo_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: gas_cylinder_storage_hstr_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# カレンダーマスタ
  - name: stg_calendar_master_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: calendar_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# ポストNo代表者マスタ
  - name: stg_postnodaihyosya_master_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: post_no_spokesperson_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 基準表鋼材手配
  - name: stg_kijunhyo_kouzaitehai_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: std_tbl_steel_prct_arng_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 基準表在庫
  - name: stg_kijunhyo_zaiko_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: std_tbl_stock_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 基準表仕掛内示
  - name: stg_kijunhyo_shikakenaiji_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: std_tbl_in_prcs_mtl_rqi_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 計量票
  - name: stg_keiryohyo_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: measurement_vote_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 工場間･工場内調達依頼
  - name: stg_chotatsuirai_kojokannai_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: factory_procurement_rqe_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 鋼材手配
  - name: stg_kozaitehai_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: steel_arrangement_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 在庫取引
  - name: stg_zaikotorihiki_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: stock_trade_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 資材内示
  - name: stg_sizainaiji_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: mtl_mtl_rqi_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 修理Y品番マスタ
  - name: stg_syuriyhinban_master_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: repair_y_item_no_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 所番地_鉄
  - name: stg_tokorobanchi_tetsu_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: location_code_iron_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 所番地_鉄以外
  - name: stg_tokorobanchi_tetsuigai_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: location_code_not_iron_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 代替品マスタ
  - name: stg_daitaihin_master_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: substitute_item_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 発注依頼
  - name: stg_hachuirai_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: odr_rqe_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 発注付加情報
  - name: stg_hachuhuka_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: odr_rqe_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 所番地廃止_鉄以外
  - name: stg_tokorobanchi_tetsuigai_haishi_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: location_abls_not_iron_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 鉄余剰材マスタ
  - name: stg_yojozai_tetsu_master_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: iron_spls_mtl_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 発注変更
  - name: stg_hachuhenko_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: odr_chg_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# Y品番変更履歴
  - name: stg_yhinbanhenko_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: y_item_no_update_hstr_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 決裁ルートマスタ
  - name: stg_kessairoute_maps_view
    config:
      tags: ["maps"]
    columns:
      - name: decision_route_id
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error