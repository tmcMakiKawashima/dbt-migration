version: 2

models:
# 品番情報
  - name: stg_hinban
    config:
      tags: ["sms-kousei"]
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - hinban
          config:
            severity: error

    columns:
      - name: hinban
        data_tests:
          - not_null:
              config:
                severity: error