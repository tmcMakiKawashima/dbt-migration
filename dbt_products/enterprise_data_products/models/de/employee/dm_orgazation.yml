version: 2
models:
# 従業員情報(兼務)
  - name: dm_empmasterknm_view
    config:
      tags: ["j-net"]
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - jcd
            - hriszcd
          config:
            severity: error
    columns:
      - name: jcd
        data_tests:
          - not_null:
              config:
                severity: error
    columns:
      - name: hriszcd
        data_tests:
          - not_null:
              config:
                severity: error
# 従業員情報(主務)
  - name: dm_empmastershm_view
    config:
      tags: ["j-net"]
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - jcd
          config:
            severity: error
    columns:
      - name: jcd
        data_tests:
          - not_null:
              config:
                severity: error