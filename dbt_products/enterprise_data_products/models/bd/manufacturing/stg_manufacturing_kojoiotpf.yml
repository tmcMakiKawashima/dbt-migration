version: 2

models:
# 電池情報（TR-M）
  - name: stg_denchijoho_trm_kojoiotpf
    config:
      tags: ["kojoiotpf"]
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - traceability_id
            - module_id
          config:
            severity: error
    columns:
      - name: traceability_id
        data_tests:
          - not_null:
              config:
                severity: error
      - name: module_id
        data_tests:
          - not_null:
              config:
                severity: error
      - name: manufacturer_cd
        data_tests:
          - not_null:
              config:
                severity: error
      - name: ldts
        data_tests:
          - not_null:
              config:
                severity: error