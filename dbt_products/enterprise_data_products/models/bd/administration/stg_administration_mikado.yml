version: 2

models:
# 入退場データ
  - name: stg_nyutaijo
    config:
      tags: ["mikado"]
    columns:
      - name: employee_cd
        tests:
          - not_null:
              config:
                severity: error
      - name: cdatetime
        tests:
          - not_null:
              config:
                severity: error
      - name: ldts
        tests:
          - not_null:
              config:
                severity: error
  - name: stg_businesstrip
    config:
      tags: ["mikado_jnet"]
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - noshucho
            - ddstart
    columns:
      - name: noshucho
        tests:
          - not_null:
              config:
                severity: error
      - name: ddstart
        tests:
          - not_null:
              config:
                severity: error
      - name: ldts
        tests:
          - not_null:
              config:
                severity: error