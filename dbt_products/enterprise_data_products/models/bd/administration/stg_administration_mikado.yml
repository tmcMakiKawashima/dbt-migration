version: 2

models:
# 入退場データ
  - name: stg_nyutaijo
    config:
      tags: ["mikado"]
    columns:
      - name: employee_cd
        tests:
          - not_null:
              config:
                severity: error
      - name: cdatetime
        tests:
          - not_null:
              config:
                severity: error
      - name: ldts
        tests:
          - not_null:
              config:
                severity: error
  - name: stg_kintai_monthly
    config:
      tags: ["mikado_jnet"]
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - employee_cd
            - h2gtjkntym
            - h2knmtimcd
    columns:
      - name: employee_cd
        tests:
          - not_null:
              config:
                severity: error
      - name: h2gtjkntym
        tests:
          - not_null:
              config:
                severity: error
      - name: h2knmtimcd
        tests:
          - not_null:
              config:
                severity: error
      - name: ldts
        tests:
          - not_null:
              config:
                severity: error