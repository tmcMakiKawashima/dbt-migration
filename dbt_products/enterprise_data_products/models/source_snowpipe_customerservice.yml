version: 2

sources:
  - name: snowpipe_db_customerservice
    database: snowpipe_db
    schema: customerservice
    freshness:
      warn_after: { count: 1, period: hour }
      error_after: { count: 24, period: hour }
    loaded_at_field: ldts

    tables:
    # SIAS
      - name: raw_ktrla05azz0kvw0001
        identifier: ktrla05azz0kvw0001
        columns:
          - name: raw_data
            data_tests:
              - fixed_length:
                  length: 61