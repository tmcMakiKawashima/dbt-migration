dagster_dbt:
  target: LOCAL
  outputs:
    LOCAL:
      type: snowflake
      account: rf70500.ap-northeast-1.aws
      
      # 1. 準本番は個人ユーザーをシステムユーザーとして使用することはアプリインフラ的にNGのため、
      # システムユーザーをしようすること

      # 2.準本番はスキーマを作成する権限が通常のシステムユーザーには付与されないため、自動生成できない。
      # そのため、スキーマについては、予め、スキーマを手動で作成しておく必要がある。

      # User/password auth
      user: sajp1.dagster_poc_sys_usr
      password: TKzDlg7aP#uA

      role: FIVETRAN_ROLE
      database: FIVETRAN_DATABASE
      warehouse: FIVETRAN_WAREHOUSE
      schema: ORACLERDS_LZDATA01_DAGSTER_DXPFY2D

    PROD:
      type: snowflake
      account: "{{ env_var('SNOWFLAKE_ACCOUNT_SA', '') }}"

      # User/password auth
      user: "{{ env_var('SNOWFLAKE_POC_USER', '') }}"
      password: "{{ env_var('SNOWFLAKE_POC_USER_PASSWORD', '') }}"
      authenticator: username_password_mfa
      role: ACCOUNTADMIN
      database: FIVETRAN_DATABASE
      warehouse: FIVETRAN_WAREHOUSE
      schema: ORACLERDS_LZDATA01_DAGSTER_DXPFY2D