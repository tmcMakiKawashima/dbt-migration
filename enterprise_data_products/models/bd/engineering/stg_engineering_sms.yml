version: 2

models:
# カラーNo
  - name: stg_color_no_sms
    config:
      tags: ["sms"]
    columns:
      - name: gclrno
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error