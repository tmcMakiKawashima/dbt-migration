version: 2

sources:
  - name: katashiki_db_basespec
    database: katashiki_db
    schema: basespec
    tables:
    # 他車種共用部品の設変調整支援(SMS利活用)
      - name: raw_dm_syasyu_katashiki_syaryokoujyo
        identifier: dm_syasyu_katashiki_syaryokoujyo

  - name: katashiki_db_spec
    database: katashiki_db
    schema: spec
    tables:
    # 生産仕様実績見える化
      - name: raw_dm_syasyu_katashiki_specmaster
        identifier: dm_syasyu_katashiki_specmaster