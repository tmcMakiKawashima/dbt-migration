version: 2

models:
# 車種(トヨタ車種限定)
  - name: stg_syasyu_aisac_tmc
    config:
        tags: ["aisac"]
    columns:
      - name: syasyu
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error