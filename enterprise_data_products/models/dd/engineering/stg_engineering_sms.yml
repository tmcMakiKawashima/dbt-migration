version: 2

models:
# 車種(トヨタ車種限定)
  - name: stg_syasyu_tmc_sms
    config:
        tags: ["sms"]
    columns:
      - name: syasyu
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error
# 品番情報(トヨタ車種限定)
  - name: stg_hinban_tmc_sms
    config:
      tags: ["sms"]
    columns:
      - name: hinban
        data_tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error