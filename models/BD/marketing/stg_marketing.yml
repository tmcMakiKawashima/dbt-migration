version: 2

models:
# 新車明細
  - name: stg_shinsyameisai
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - dlrcd
            - cuno
            - edano
            - ym
          config:
            severity: error

    columns:
      - name: dlrcd
        tests:
          - not_null:
              config:
                severity: error
      - name: cuno
        tests:
          - not_null:
              config:
                severity: error
      - name: edano
        tests:
          - not_null:
              config:
                severity: error
      - name: ym
        tests:
          - not_null:
              config:
                severity: error
