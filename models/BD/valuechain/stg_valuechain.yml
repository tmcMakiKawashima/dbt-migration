version: 2
models:
#　部品歴
  - name: stg_buhinreki
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - nyukono
            - meisaino
          config:
            severity: error
    columns:
      - name: nyukono
        tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error