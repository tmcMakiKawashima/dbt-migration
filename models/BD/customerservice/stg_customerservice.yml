version: 2
models:
# 市情報
  - name: stg_shijoho
    columns:
      - name: ftr_no
        tests:
          - unique:
              config:
                severity: error
          - not_null:
              config:
                severity: error